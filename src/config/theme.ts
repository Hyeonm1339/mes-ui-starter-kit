/**
 * 디자인 토큰 오버라이드
 *
 * 사용법:
 *   1. 아래 백틱(`) 사이에 CSS 변수를 붙여넣으세요.
 *   2. 비워두면 src/index.css의 기본값이 사용됩니다.
 *
 * 예시:
 *   export const themeOverrideCss = `
 *     :root { --primary: oklch(0.5 0.2 250); }
 *     .dark { --primary: oklch(0.6 0.2 250); }
 *   `
 */
export const themeOverrideCss = ``
